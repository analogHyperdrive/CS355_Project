<!DOCTYPE html>
<html>
<head>
    <title>Query 4</title>
</head>
<body>
<h1>A query using a subquery EXISTS or NOT EXISTS</h1>
Query:<br />
SELECT c.first_name, c.last_name FROM player_character c<br />
WHERE EXISTS(<br />
&nbsp &nbsp SELECT * FROM player_character x<br />
&nbsp &nbsp WHERE c.character_id = x.main_id<br />
&nbsp &nbsp AND c.character_id != x.character_id); <br /> <br />
Explanation: Returns a list of all characters that have an assoicated alt, returns the same output as Query #3 <br /> <br />
Result:
<table border="1">
    <tr><th>first_name</th><th>last_name</th></tr>
    <% for(var i = 0; i < result.length; i++) { %>
    <tr>
        <td>
            <%= result[i].first_name %>
        </td>
        <td>
            <%= result[i].last_name %>
        </td>
    </tr>
    <% } %>
</table>
</body>
</html>